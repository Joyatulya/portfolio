import { describe, it } from "vitest";
import { InteractiveEngine } from ".";

describe('Interactive Engine', async () => {
  it('init', async () => {
    const interactiveEngine = new InteractiveEngine()
    const pt = { hb: 120, isMale: true }
  })
})
